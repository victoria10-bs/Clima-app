name: Despliegue Automatizado

on:
  push:
    branches:
      - main  # Se ejecuta cuando subes cambios a la rama principal

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    # ¡ESTA ES LA PARTE CLAVE! 
    # Al definir esto, GitHub activa la sección de "Despliegues"
    environment:
      name: production
      url: https://tu-sitio-web.com # Opcional: El enlace a tu web para que aparezca un botón directo

    steps:
      - name: Descargar código fuente
        uses: actions/checkout@v4

      - name: Ejecutar comandos de despliegue
        run: |
          echo "Aquí van los comandos para compilar y subir tu proyecto..."